# Plan

> The current iteration's living plan. **The loop overwrites this file
> every iteration.** Keep it under one screen. If you find yourself
> writing more than ~30 lines, move detail into `tickets/` and link.

## Goal

(One sentence. What is the loop trying to accomplish overall? Pull this
from `.ralph/specs/`.)

## Status

- **Done so far:**
  - (bullets — what's already shipped, verifiable in the codebase)
- **In progress (this iteration):**
  - (the single ticket / change being worked on right now)
- **Up next:**
  - (the 1–3 tickets that come after, in priority order)
- **Blocked:**
  - (anything waiting on a human, an external service, or a missing spec)

## Notes

(Anything the next iteration needs to know that doesn't fit elsewhere.
Examples: "the test runner is `pnpm test` not `npm test`",
"refactor of module X was deferred to a follow-up", etc.)
